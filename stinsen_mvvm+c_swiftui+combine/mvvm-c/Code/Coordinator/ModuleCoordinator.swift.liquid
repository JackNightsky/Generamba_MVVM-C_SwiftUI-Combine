import Foundation
import SwiftUI
import Stinsen

final class {{ module_info.name }}ModuleCoordinator: {{ module_info.name }}Coordinator, NavigationCoordinatable {
    typealias NavigationStack = Stinsen.NavigationStack

    var stack: NavigationStack<{{ module_info.name }}ModuleCoordinator> = NavigationStack(initial: \.start)
    @Root var start = makeStart

    func makeStart() -> some View {
        let viewModel = {{ module_info.name }}ModuleViewModel()
        return {{ module_info.name }}View(viewModel: viewModel)
    }
}
